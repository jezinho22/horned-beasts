import { beastData } from "./data"


function SearchOptions(beast) {
  return (
    // provide the options for the select box
    {}
    <option value={beast}>{beast}</option>
  )
}

export default SearchOptions